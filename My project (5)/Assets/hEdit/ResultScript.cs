using UnityEngine;

public class ResultDataScript : MonoBehaviour
{
    public static int life;        // 残りライフ
    public static float time;      // 残りタイム
    public static int correct;     // 見つけた間違い数
    public static int totalCorrect; // 全部の間違い数


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
